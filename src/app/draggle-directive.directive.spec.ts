import { DraggleDirectiveDirective } from './draggle-directive.directive';

describe('DraggleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
